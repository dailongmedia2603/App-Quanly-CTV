import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const CreateEmailContentTab = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Tạo nội dung Mail</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Tính năng đang được phát triển. AI sẽ giúp bạn tạo nội dung email chuyên nghiệp tại đây.</p>
      </CardContent>
    </Card>
  );
};

export default CreateEmailContentTab;