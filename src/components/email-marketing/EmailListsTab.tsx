import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const EmailListsTab = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Danh sách mail</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Tính năng đang được phát triển. Bạn sẽ có thể quản lý danh sách email của mình tại đây.</p>
      </CardContent>
    </Card>
  );
};

export default EmailListsTab;